package scala_exercises.week02

//Provide a fully parenthized version of
// a + b ^? c ^? d less a ==> b | c

// precedence
// (all letters)
// |
// ^
// &
// <>
// = !
// :
// + -
// * / %
// (all other special characters)

//

object exercise06Lecture27EvaluationAndOperators extends App {

  // (( a + b ) ^? ( c ^? d ))  less (( a ==> b ) | c )

  // Every binary operation needs to be put into parentheses, but the
  // structure of the expression shold not change
}
